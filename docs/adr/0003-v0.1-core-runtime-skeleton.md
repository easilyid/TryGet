# Keep V0.1 to the core runtime skeleton

MyTryGetFramework V0.1 is intentionally limited to the core runtime shape: World, Entity, Aspect, System, Adapter, Phase, and SystemGroup. We do not bring in the full editor/tooling/resource/hot-reload stack at this stage, because the first release should validate the composition model and scheduling boundaries before the framework grows into a broader platform.
