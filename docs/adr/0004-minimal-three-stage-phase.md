# Use the minimal three-stage Phase model

MyTryGetFramework V0.1 fixes World execution into a minimal three-stage Phase model: Enter, Update, and Exit. That gives the runtime a clear lifecycle without overfitting the first version with custom phase graphs or gameplay-specific stage taxonomies.
