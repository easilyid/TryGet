# Add query, event, and tag to the core glossary

MyTryGetFramework V0.1 includes a minimal but explicit vocabulary for Query, Event, and Tag. Query is the System-side composition filter over Entity Aspect sets; Event is kept as a layered notification concept; Tag is a presence-only marker. This keeps the runtime model expressive without dragging in a full scripting, messaging, or metadata platform.
