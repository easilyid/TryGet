Status: done (2026/06/18, Generator tests 14/14; Core shadow build 0 warnings/0 errors; Tests shadow csproj 459/459)

# Source Generator Runtime Contract

## Parent

.scratch/core-foundation-iteration/PRD.md

## What to build

Harden the `[Module]` and `[EventHandler]` Source Generator contract against runtime drift. This slice should verify generated manifest output, compile-time diagnostics, runtime registry metadata, multi-entry assembly behavior, dual-trigger initialization, and the `Generators~/` source-of-truth plus dll artifact workflow.

## Acceptance criteria

- [x] Legal `[Module]` input generates ModuleRegistry metadata and host.Register calls using the declared service interface and implementation type.
- [x] Legal `[EventHandler]` input generates EventHandlerRegistry metadata and bus.Subscribe calls using the event struct type and static handler.
- [x] No manifest source is emitted when an assembly has no relevant attributes.
- [x] Diagnostics TG0001-TG0006 are reported for invalid Module/EventHandler inputs with source locations and without producing broken registration code.
- [x] Multiple modules or handlers in one assembly produce one execution registration but one metadata entry per module or handler.
- [x] Generated code references the current runtime registry names and compiles against the runtime test stubs.
- [x] The generator test suite passes, and behavior-changing edits are rebuilt through the generator build script so the Unity-loaded dll is synced.
- [x] Documentation states that `Generators~/` is source of truth and `Runtime/Core/Generators` contains the Unity-required build artifact.

## Blocked by

- .scratch/core-foundation-iteration/issues/02-modulesystem-contract-hardening.md
- .scratch/core-foundation-iteration/issues/03-eventmodule-lifecycle-and-diagnostics.md

## Comments

> *This was generated by AI during triage.*

Completed Source Generator runtime contract verification on 2026/06/18.

Implementation notes:

- Existing generator source and tests already matched the runtime contract for `[Module]` and `[EventHandler]` manifest generation, TG0001-TG0006 diagnostics, metadata snapshots, multi-entry assembly behavior, and current `ModuleRegistry` / `EventHandlerRegistry` names.
- Existing `Generators~/README.md` already documents `Generators~/` as source of truth, `Runtime/Core/Generators` as the Unity-required dll artifact location, and the test/build/Release-only sync workflow.
- No generator behavior was changed, so the Release build/sync script was not run and the Unity-loaded dll was intentionally not churned.

Verification:

- `dotnet test MyTryGetFramework/Assets/MyTryGetFramework/Generators~/MyTryGetFramework.SourceGenerator.sln` passed with 14 tests.
- Confirmed `MyTryGetFramework/Assets/MyTryGetFramework/Runtime/Core/Generators/MyTryGetFramework.SourceGenerator.dll` exists.
- `dotnet build ServerProject/MyTryGetFramework.Core/MyTryGetFramework.Core.csproj` passed with 0 warnings and 0 errors.
- `dotnet test ServerProject/MyTryGetFramework.Tests/MyTryGetFramework.Tests.csproj` passed with 459 tests passed, 0 failed, 0 skipped.
