Status: done (2026/06/18, Core shadow build 0 warnings/0 errors; Tests shadow csproj 459/459)

# PoolModule Diagnostics And Safety

## Parent

.scratch/core-foundation-iteration/PRD.md

## What to build

Harden PoolModule as the Core generic object-pool service. This slice should protect pool creation, prewarm behavior, Rent/Return semantics, reset hooks, DestroyPool, Shutdown cleanup, diagnostics, and debug double-return detection while keeping GameObject pooling out of Core.

## Acceptance criteria

- [x] GetOrCreatePool returns the same pool for the same type and validates factory and initialSize inputs.
- [x] Initial prewarm fills idle objects and affects diagnostics consistently.
- [x] Rent uses idle objects before factory creation and updates total rented, hit, miss, active, idle, and peak counters.
- [x] Return invokes the reset hook, updates diagnostics, and ignores null returns according to the public contract.
- [x] DEBUG or UNITY_ASSERTIONS builds detect returning an object not currently active in that pool.
- [x] DestroyPool removes only the selected type pool; Shutdown clears all pools.
- [x] Tests avoid Unity GameObject assumptions and keep the Core pool generic and pure C#.

## Blocked by

- .scratch/core-foundation-iteration/issues/01-core-contract-baseline.md

## Comments

> *This was generated by AI during triage.*

Completed PoolModule diagnostics and safety hardening on 2026/06/18.

Implementation notes:

- Existing PoolModule behavior already matched the Core generic pool contract for creation, reuse, validation, Return hooks, null return handling, DestroyPool, Shutdown cleanup, diagnostics, and DEBUG/UNITY_ASSERTIONS double-return protection.
- Added a diagnostics regression test proving prewarmed objects count as hits when rented and update active, idle, and peak counters consistently.
- No Unity GameObject or Adapter assumptions were introduced.

Verification:

- `dotnet test ServerProject/MyTryGetFramework.Tests/MyTryGetFramework.Tests.csproj --filter "FullyQualifiedName~PoolModuleTests|FullyQualifiedName~PoolDiagnosticsTests"` passed with 31 tests.
- `dotnet build ServerProject/MyTryGetFramework.Core/MyTryGetFramework.Core.csproj` passed with 0 warnings and 0 errors.
- `dotnet test ServerProject/MyTryGetFramework.Tests/MyTryGetFramework.Tests.csproj` passed with 459 tests passed, 0 failed, 0 skipped.
